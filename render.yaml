services:
  - type: web
    name: yolo-detector
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn detector:app
    envVars:
      # CAMBIO CLAVE: Usar RENDER_PYTHON_VERSION
      - key: RENDER_PYTHON_VERSION
        value: 3.10.14 # O 3.10.14, pero 3.11 es más nuevo y compatible